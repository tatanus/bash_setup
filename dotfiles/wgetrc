# Default download directory
# dir_prefix = ~/Downloads

# Use system user-agent (mimic a browser)
user_agent = Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/91.0.4472.124 Safari/537.36

# Increase retries for unstable connections
tries = 10
waitretry = 5

# Enable timestamping (avoid re-downloading unchanged files)
timestamping = on

# Limit download speed to prevent excessive bandwidth usage
# limit_rate = 1M

# Enable passive FTP mode (for better firewall/NAT compatibility)
passive_ftp = on

# Follow HTTP redirects automatically
follow_ftp = on

# Continue interrupted downloads
continue = on

# Use proxychains if proxy is enabled
# http_proxy = socks5h://127.0.0.1:9050
# https_proxy = socks5h://127.0.0.1:9050
# use_proxy = on

# Disable certificate checks (if necessary for pentesting)
check_certificate = off