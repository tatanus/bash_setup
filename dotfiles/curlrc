# Use custom user agent
user-agent = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/91.0.4472.124 Safari/537.36"

# Set a timeout for connections
connect-timeout = 30

# Use IPv4 only (avoids IPv6 issues)
ipv4

# Follow redirects automatically
location

# Enable retry on failure
retry = 5
retry-delay = 3

# Silent mode (hide progress bar)
silent

# Debugging: Output response headers
# dump-header = ~/curl_headers.txt

# Save cookies
cookie-jar = ~/cookies.txt
cookie = ~/cookies.txt

# Use proxychains for network traffic
# proxy = socks5h://127.0.0.1:9050

# Disable certificate checks (useful for pentesting)
insecure